# src

Source for the book store's three microservices (`catalog/`, `order/`, `front_end/`), the
`client/`, and gRPC contracts (`protos/`).

See the [top-level README](../README.md) for how to install dependencies and run the app
locally, and [deploy/README.md](deploy/README.md) for running it on a cloud VM.
